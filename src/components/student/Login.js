import React from 'react';

import { Container } from '@material-ui/core';

const StudentLogin = () => {
    return (
        <Container fixed>
            
        </Container>
    );
};

export default StudentLogin;