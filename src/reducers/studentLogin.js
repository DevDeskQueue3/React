const initialState = {
    student: {},
    isFetching: false,
    error: ""
}

export const studentLogin = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}